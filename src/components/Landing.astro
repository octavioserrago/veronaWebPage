<div class="Landing w-dvw h-screen flex flex-col items-center">
    <h1
        class="text-3xl mt-8 md:text-5xl lg:text-6xl text-white text-center md:mt-8 lg:mt-8 xl:mt-20 font-bold"
        id="mainTitle"
    >
        MARMOLERÍA VERONA
    </h1>
    <p
        class="subtitle text-center text-white font-normal text-base md:text-3xl lg:text-3xl xl:mt-4"
    >
        Transformando espacios, dando vida a tus proyectos
    </p>
    <a id="wppButton"
        ><button
            class="mt-4 px-4 py-2 rounded-full border border-yellow-400 text-yellow-400 bg-black hover:bg-yellow-500 hover:text-white transition-colors duration-50 ease-in-out"
        >
            Contáctanos
        </button></a
    >
    <div id="modal" class="modal">
        <div class="modal-content">
            <h2>¿A qué sede te gustaría contactar?</h2>
            <button id="fabricaButton" class="modal-button">Fábrica</button>
            <button id="localButton" class="modal-button">Local</button>
            <button id="azarButton" class="modal-button">Al azar</button>
        </div>
    </div>
</div>

<style>
    .subtitle {
        font-family: "San Francisco Display Ultralight";
    }
    #mainTitle {
        font-family: "San Francisco Display Semibold";
    }
    .Landing {
        background-image: url("/assets/Landing01.png");
        background-size: cover;
        background-repeat: no-repeat;
        background-position: center;
    }
    .modal {
        display: none;
        position: fixed;
        z-index: 1;
        left: 0;
        top: 0;
        width: 100%;
        height: 100%;
        overflow: auto;
        background-color: rgb(0, 0, 0);
        background-color: rgba(0, 0, 0, 0.4);
        justify-content: center;
        align-items: center;
    }
    .modal-content {
        border-radius: 5px;
        background-color: white;
        margin: auto;
        padding: 20px;
        border: 1px solid #888;
        width: 80%;
        max-width: 400px;
        text-align: center;
    }
    .modal-button {
        margin: 10px;
        padding: 10px 20px;
        cursor: pointer;
        background-color: #ffaa00;
        color: black;
        border: none;
        border-radius: 5px;
        transition: background-color 0.3s;
    }
    .modal-button:hover {
        background-color: #e39700;
    }
</style>

<script>
    document
        .getElementById("wppButton")
        .addEventListener("click", function (event) {
            event.preventDefault();
            document.getElementById("modal").style.display = "flex";
        });

    document
        .getElementById("fabricaButton")
        .addEventListener("click", function () {
            window.location.href =
                "https://wa.me/1159906984?text=Hola,%20vengo%20desde%20la%20pagina%20web,%20tengo%20una%20consulta!";
        });

    document
        .getElementById("localButton")
        .addEventListener("click", function () {
            window.location.href =
                "https://wa.me/1162922173?text=Hola,%20vengo%20desde%20la%20pagina%20web,%20tengo%20una%20consulta!";
        });

    document
        .getElementById("azarButton")
        .addEventListener("click", function () {
            let randomValue = Math.random();
            console.log("Random Value:", randomValue);
            let randomLink =
                randomValue < 0.5
                    ? "https://wa.me/1159906984?text=Hola,%20vengo%20desde%20la%20pagina%20web,%20tengo%20una%20consulta!"
                    : "https://wa.me/1162922173?text=Hola,%20vengo%20desde%20la%20pagina%20web,%20tengo%20una%20consulta!";
            console.log("Selected Link:", randomLink);
            window.location.href = randomLink;
        });

    window.onclick = function (event) {
        if (event.target == document.getElementById("modal")) {
            document.getElementById("modal").style.display = "none";
        }
    };
</script>
